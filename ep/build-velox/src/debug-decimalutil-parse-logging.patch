diff --git a/velox/type/DecimalUtil.cpp b/velox/type/DecimalUtil.cpp
index 057eacd38..8624515e6 100644
--- a/velox/type/DecimalUtil.cpp
+++ b/velox/type/DecimalUtil.cpp
@@ -15,6 +15,7 @@
  */
 
 #include "velox/type/DecimalUtil.h"
+#include <glog/logging.h>
 #include "velox/type/HugeInt.h"
 
 namespace facebook::velox {
@@ -50,6 +51,11 @@ std::string formatDecimal(uint8_t scale, int128_t unscaledValue) {
   return fmt::format(
       "{}{}{}", isNegative ? "-" : "", integralPart, fractionString);
 }
+
+std::string formatInt128ForLog(int128_t value) {
+  return fmt::format(
+      "upper={}, lower={}", HugeInt::upper(value), HugeInt::lower(value));
+}
 } // namespace
 
 std::string DecimalUtil::toString(int128_t value, const Type& type) {
@@ -278,6 +284,16 @@ Status DecimalUtil::parseStringToDecimalComponents(
     return Status::UserError("Value is not a number. " + status.message());
   }
 
+  LOG(WARNING) << "DecimalUtil::parseStringToDecimalComponents input='"
+               << s.getString() << "' toScale=" << toScale
+               << " wholeDigits='" << decimalComponents.wholeDigits
+               << "' fractionalDigits='" << decimalComponents.fractionalDigits
+               << "' exponent="
+               << (decimalComponents.exponent.has_value()
+                       ? std::to_string(decimalComponents.exponent.value())
+                       : "null")
+               << " sign=" << static_cast<int32_t>(decimalComponents.sign);
+
   // Count number of significant digits (without leading zeros).
   const size_t firstNonZero =
       decimalComponents.wholeDigits.find_first_not_of('0');
@@ -304,6 +320,11 @@ Status DecimalUtil::parseStringToDecimalComponents(
   } else {
     const auto exponent = decimalComponents.exponent.value();
     parsedScale = -exponent + fractionSize;
+    LOG(WARNING) << "DecimalUtil::parseStringToDecimalComponents exponent branch"
+                 << " input='" << s.getString() << "' exponent=" << exponent
+                 << " fractionSize=" << fractionSize
+                 << " initialParsedScale=" << parsedScale
+                 << " toScale=" << toScale;
     // Truncate the fractionalDigits.
     if (parsedScale > toScale) {
       if (-exponent >= toScale) {
@@ -313,6 +334,10 @@ Status DecimalUtil::parseStringToDecimalComponents(
         }
         decimalComponents.fractionalDigits = "";
         parsedScale -= fractionSize;
+        LOG(WARNING)
+            << "DecimalUtil::parseStringToDecimalComponents dropped fractional digits"
+            << " input='" << s.getString() << "' roundUp=" << roundUp
+            << " parsedScale=" << parsedScale;
       } else {
         const auto reduceDigits = exponent + toScale;
         if (fractionSize > reduceDigits &&
@@ -323,12 +348,23 @@ Status DecimalUtil::parseStringToDecimalComponents(
             decimalComponents.fractionalDigits.data(),
             std::min(reduceDigits, fractionSize));
         parsedScale -= fractionSize - decimalComponents.fractionalDigits.size();
+        LOG(WARNING)
+            << "DecimalUtil::parseStringToDecimalComponents truncated fractional digits"
+            << " input='" << s.getString() << "' reduceDigits=" << reduceDigits
+            << " keptFractionalDigits='" << decimalComponents.fractionalDigits
+            << "' roundUp=" << roundUp << " parsedScale=" << parsedScale;
       }
     }
   }
 
   VELOX_RETURN_NOT_OK(parseHugeInt(decimalComponents, out));
 
+  LOG(WARNING) << "DecimalUtil::parseStringToDecimalComponents parsed huge int"
+               << " input='" << s.getString() << "' out{"
+               << formatInt128ForLog(out) << "} parsedPrecision="
+               << parsedPrecision << " parsedScale=" << parsedScale
+               << " roundUp=" << roundUp;
+
   if (roundUp) {
     bool overflow = __builtin_add_overflow(out, 1, &out);
     if (UNLIKELY(overflow)) {
@@ -349,9 +385,19 @@ Status DecimalUtil::parseStringToDecimalComponents(
     if (UNLIKELY(overflow)) {
       return Status::UserError("Value too large.");
     }
+    LOG(WARNING)
+        << "DecimalUtil::parseStringToDecimalComponents rescaled negative parsedScale"
+        << " input='" << s.getString() << "' scaleAdjustment="
+        << (-parsedScale + toScale) << " out{" << formatInt128ForLog(out)
+        << "}";
     parsedPrecision -= parsedScale;
     parsedScale = toScale;
   }
+
+  LOG(WARNING) << "DecimalUtil::parseStringToDecimalComponents result input='"
+               << s.getString() << "' parsedPrecision=" << parsedPrecision
+               << " parsedScale=" << parsedScale << " out{"
+               << formatInt128ForLog(out) << "}";
   return Status::OK();
 }
 
diff --git a/velox/type/DecimalUtil.h b/velox/type/DecimalUtil.h
index 89bdddafc..90ca4e555 100644
--- a/velox/type/DecimalUtil.h
+++ b/velox/type/DecimalUtil.h
@@ -17,6 +17,7 @@
 #pragma once
 
 #include <charconv>
+#include <glog/logging.h>
 #include <string>
 #include "velox/common/base/CheckedArithmetic.h"
 #include "velox/common/base/CountBits.h"
@@ -168,21 +169,50 @@ class DecimalUtil {
     int128_t rescaledValue = inputValue;
     auto scaleDifference = toScale - fromScale;
     bool isOverflow = false;
+    LOG(WARNING) << "DecimalUtil::rescaleWithRoundUp enter"
+                 << " inputValue=" << std::to_string(inputValue)
+                 << " fromPrecision=" << fromPrecision
+                 << " fromScale=" << fromScale
+                 << " toPrecision=" << toPrecision
+                 << " toScale=" << toScale
+                 << " scaleDifference=" << scaleDifference;
     if (scaleDifference >= 0) {
+      LOG(WARNING) << "DecimalUtil::rescaleWithRoundUp multiply branch"
+                   << " inputValue=" << std::to_string(inputValue)
+                   << " scaleDifference=" << scaleDifference;
       isOverflow = __builtin_mul_overflow(
           rescaledValue,
           DecimalUtil::kPowersOfTen[scaleDifference],
           &rescaledValue);
+      LOG(WARNING) << "DecimalUtil::rescaleWithRoundUp multiply result"
+                   << " rescaledValue=" << std::to_string(rescaledValue)
+                   << " isOverflow=" << isOverflow;
     } else {
       scaleDifference = -scaleDifference;
       const auto scalingFactor = DecimalUtil::kPowersOfTen[scaleDifference];
+      LOG(WARNING) << "DecimalUtil::rescaleWithRoundUp divide branch"
+                   << " inputValue=" << std::to_string(inputValue)
+                   << " normalizedScaleDifference=" << scaleDifference
+                   << " scalingFactor=" << std::to_string(scalingFactor)
+                   << " rescaledValueBeforeDivide="
+                   << std::to_string(rescaledValue);
       rescaledValue /= scalingFactor;
       int128_t remainder = inputValue % scalingFactor;
+      LOG(WARNING) << "DecimalUtil::rescaleWithRoundUp divide intermediate"
+                   << " rescaledValueAfterDivide="
+                   << std::to_string(rescaledValue)
+                   << " remainder=" << std::to_string(remainder)
+                   << " positiveRoundCondition="
+                   << (inputValue >= 0 && remainder >= scalingFactor / 2)
+                   << " negativeRoundCondition="
+                   << (remainder <= -scalingFactor / 2);
       if (inputValue >= 0 && remainder >= scalingFactor / 2) {
         ++rescaledValue;
       } else if (remainder <= -scalingFactor / 2) {
         --rescaledValue;
       }
+      LOG(WARNING) << "DecimalUtil::rescaleWithRoundUp divide result"
+                   << " rescaledValue=" << std::to_string(rescaledValue);
     }
     // Check overflow.
     if (!valueInPrecisionRange(rescaledValue, toPrecision) || isOverflow) {
@@ -192,6 +222,9 @@ class DecimalUtil {
           toPrecision,
           toScale);
     }
+    LOG(WARNING) << "DecimalUtil::rescaleWithRoundUp exit"
+                 << " rescaledValue=" << std::to_string(rescaledValue)
+                 << " isOverflow=" << isOverflow;
     output = static_cast<TOutput>(rescaledValue);
     return Status::OK();
   }
@@ -584,6 +617,12 @@ class DecimalUtil {
     VELOX_RETURN_NOT_OK(parseStringToDecimalComponents(
         s, toScale, parsedPrecision, parsedScale, out));
 
+    LOG(WARNING) << "DecimalUtil::castFromString parsed input='"
+                 << s.getString() << "' parsedPrecision=" << parsedPrecision
+                 << " parsedScale=" << parsedScale
+                 << " out=" << std::to_string(out)
+                 << " toPrecision=" << toPrecision << " toScale=" << toScale;
+
     const auto status = rescaleWithRoundUp<int128_t, T>(
         out,
         std::min(
@@ -593,6 +632,9 @@ class DecimalUtil {
         toPrecision,
         toScale,
         decimalValue);
+    LOG(WARNING) << "DecimalUtil::castFromString rescale status input='"
+                 << s.getString() << "' ok=" << status.ok()
+                 << " decimalValue=" << std::to_string(decimalValue);
     if (!status.ok()) {
       return Status::UserError("Value too large.");
     }
